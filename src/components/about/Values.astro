---
const values = [
  [
    {
      ref: '01',
      name: 'Kvalita',
      description:
        'Zakládáme si na precizně odvedené práci a dlouhodobých výsledcích. Každé zahradě věnujeme maximální péči, používáme kvalitní materiály a osvědčené postupy, aby výsledek byl nejen krásný, ale i trvale udržitelný.',
    },
    {
      ref: '02',
      name: 'Inovace',
      description:
        'Sledujeme moderní trendy v zahradní architektuře a péči o zeleň. Využíváme nové technologie, efektivní postupy a ekologická řešení, která šetří čas, přírodu i vaše náklady.',
    },
  ],
  [
    {
      ref: '03',
      name: 'Spolupráce',
      description:
        'Věříme, že nejlepší výsledky vznikají díky otevřené komunikaci a týmové práci. Nasloucháme vašim přáním a úzce spolupracujeme při návrhu i realizaci, aby zahrada přesně odpovídala vašim představám.',
    },
    {
      ref: '04',
      name: 'Respekt k přírodě',
      description:
        'Příroda je pro nás na prvním místě. Pracujeme s ohledem na životní prostředí, podporujeme zdravý růst rostlin a vytváříme zahrady, které jsou v rovnováze s okolím.',
    },
  ],
];
---

<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="flex flex-col gap-8 lg:gap-12">
      <h2 class="text-3xl font-medium tracking-tight sm:text-4xl">Naše hodnoty</h2>
      <div class="grid gap-8 lg:grid-cols-2">
        {
          values.map((column, index) => (
            <div class:list={['space-y-8', index === 1 && 'lg:mt-16']}>
              {column.map((value) => (
                <div class="bg-primary-500/10 dark:bg-primary-400/10 flex flex-col gap-12 rounded-3xl px-6 py-8">
                  <p class="text-sm">{value.ref}</p>
                  <div class="flex flex-col gap-4">
                    <h3 class="text-xl font-medium">{value.name}</h3>
                    <p class="text-primary-950/70 dark:text-primary-200/70 text-base sm:text-lg">
                      {value.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
